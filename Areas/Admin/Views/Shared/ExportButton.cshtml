@* Views/Shared/_ExportButton.cshtml *@
@model Tuple<string, string>

@using (Html.BeginForm("Export", Model.Item1, FormMethod.Get))
{
    <input type="hidden" name="search" value="@Model.Item2" />
    <button type="submit" class="btn btn-success mb-3">
        <i class="bi bi-file-earmark-excel"></i> Export Excel
    </button>
}
